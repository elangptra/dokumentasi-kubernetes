# Deploy Taiga Project Management di Kubernetes (Bare-Metal)

Dokumentasi ini adalah versi yang telah disempurnakan dari tutorial standar pada link berikut [dokumentasi taiga](https://serverspace.io/support/help/taiga-on-kubernetes-deploying-a-self-hosted-project-management-tool/), mengatasi berbagai isu seperti malformasi jaringan (Ingress Routing), celah otentikasi database, dan kompatibilitas broker pesan.

## 📋 Daftar Isi
- [Deploy Taiga Project Management di Kubernetes (Bare-Metal)](#deploy-taiga-project-management-di-kubernetes-bare-metal)
  - [📋 Daftar Isi](#-daftar-isi)
  - [1. Struktur Directory](#1-struktur-directory)
  - [2. Prasyarat: Penyimpanan (StorageClass)](#2-prasyarat-penyimpanan-storageclass)
    - [Setup Variable Gitlab](#setup-variable-gitlab)
      - [Navigasi Project Settings](#navigasi-project-settings)
      - [Penambahan Variabel (Add Variable)](#penambahan-variabel-add-variable)
      - [Variabel Yang Ditambahkan](#variabel-yang-ditambahkan)
    - [Setup NFS Node Master](#setup-nfs-node-master)
      - [1. install NFS](#1-install-nfs)
      - [2. buat directory sharing](#2-buat-directory-sharing)
      - [3. konfigurasi akses](#3-konfigurasi-akses)
      - [4. apply konfigurasi](#4-apply-konfigurasi)
      - [5. install nfs provisioner](#5-install-nfs-provisioner)
    - [Setup NFS Node Worker](#setup-nfs-node-worker)
  - [3. File: Beberapa konfigurasi yang dipakai pada setup Taiga](#3-file-beberapa-konfigurasi-yang-dipakai-pada-setup-taiga)
    - [3.1 Namespace.yaml](#31-namespaceyaml)
    - [3.2 Taiga-app](#32-taiga-app)
      - [1. taiga-backend.yaml](#1-taiga-backendyaml)
      - [2. taiga-celery.yaml](#2-taiga-celeryyaml)
      - [3. taiga-configmap.yaml](#3-taiga-configmapyaml)
      - [4. taiga-frontend.yaml](#4-taiga-frontendyaml)
      - [5. taiga-ingress.yaml](#5-taiga-ingressyaml)
      - [6. taiga-pvc.yaml](#6-taiga-pvcyaml)
      - [7. taiga-service.yaml](#7-taiga-serviceyaml)
    - [3.3 Redis](#33-redis)
      - [redis-deployment.yaml](#redis-deploymentyaml)
      - [redis-service.yaml](#redis-serviceyaml)
    - [3.4 Rabbitmq](#34-rabbitmq)
      - [rabbit-deployment.yaml](#rabbit-deploymentyaml)
      - [rabbit-service.yaml](#rabbit-serviceyaml)
    - [3.5 Postgres](#35-postgres)
      - [postgress-pvc.yaml](#postgress-pvcyaml)
      - [postgress.yaml](#postgressyaml)
    - [3.6 ESO](#36-eso)
      - [taiga-app-secret.yaml](#taiga-app-secretyaml)
      - [taiga-gitlab-token.yaml](#taiga-gitlab-tokenyaml)
      - [taiga-secret-store.yaml](#taiga-secret-storeyaml)
  - [4. RUN: Menjalankan yaml, verifikasi instalasi, dan menampilkan GUI Taiga menggunakan ingress](#4-run-menjalankan-yaml-verifikasi-instalasi-dan-menampilkan-gui-taiga-menggunakan-ingress)
    - [4.1 Membuat Sandi App Gmail](#41-membuat-sandi-app-gmail)
      - [1. Masuk ke Pengaturan Akun Google](#1-masuk-ke-pengaturan-akun-google)
      - [2. Buka Menu Keamanan](#2-buka-menu-keamanan)
      - [3. Navigasi ke Sandi Aplikasi](#3-navigasi-ke-sandi-aplikasi)
      - [4. Buat Sandi Khusus Aplikasi](#4-buat-sandi-khusus-aplikasi)
      - [5. Simpan Sandi Aplikasi](#5-simpan-sandi-aplikasi)
    - [4.2 verifikasi instalasi](#42-verifikasi-instalasi)
      - [- Validasi POD](#--validasi-pod)
      - [- Validasi ingress](#--validasi-ingress)
      - [- Validasi service](#--validasi-service)
      - [- Validasi ConfigMap](#--validasi-configmap)
      - [- Validasi secrets](#--validasi-secrets)
      - [- Validasi Akun](#--validasi-akun)
    - [4.3 Mengakses halaman taiga](#43-mengakses-halaman-taiga)
  - [5. FIX : Menangi Error dan Bug pada app taiga di cluster baremetal](#5-fix--menangi-error-dan-bug-pada-app-taiga-di-cluster-baremetal)
    - [5.1 Error code 400](#51-error-code-400)



## 1. Struktur Directory

```text
.
├── Taiga-App
│   └── taiga-backend.yaml
│   └── taiga-celery.yaml
│   └── taiga-configmap.yaml
│   └── taiga-frontend.yaml
│   └── taiga-ingress.yaml
│   └── taiga-pvc.yaml
│   └── taiga-service.yaml
├── Redis
|   └── redis-deployment.yaml
|   └── redis-service.yaml
├── RabbitMQ
|   └── rabit-deployment.yaml
|   └── rabit-service.yaml
├── Postgres
|   └── postgres.yaml
|   └── postgres-pvc.yaml
├── eso
|   └── taiga-app-secret.yaml
|   └── taiga-gitlab-token.yaml
|   └── taiga-secret-store.yaml
.
```

## 2. Prasyarat: Penyimpanan (StorageClass)

karena deployment taiga dilakukan menggunakan argocd, yang harus disiapkan adalah ESO yang dapat dilihat pada dokumentasi [external secret operator](3.%20instalasi-external-secret-operator.md), setelah itu membuat variable pada gitlab yang nanti akan digunakan eso,
tahapan pembuatan variable seperti berikut:

### Setup Variable Gitlab

Langkah ini bertujuan menyimpan data sensitif (seperti kredensial registry) agar dapat digunakan oleh pipeline atau External Secrets tanpa terekspos dalam kode sumber.

#### Navigasi Project Settings
1.  Buka halaman utama proyek `kubernetes-taiga`.
2.  Pada sidebar kiri, pilih **Settings** > **CI/CD**.
3.  Cari bagian **Variables** dan klik tombol **Expand**.

#### Penambahan Variabel (Add Variable)
Klik **Add variable** dan buat entri berikut:

#### Variabel Yang Ditambahkan
Aplikasi Taiga membutuhkan 5 variable, yaitu

>* **Key:** `EMAIL_HOST_PASSWORD`
>* **Value:** `<SANDI_APLIKASI_GOOGLE>`
>* **Flags:** Mask variable (Wajib).
  
>* **Key:** `POSTGRES_PASSWORD`
>* **Value:** `cGFzc3dvcmQ=`
>* **Flags:** Mask variable (Wajib).

>* **Key:** `TAIGA_ADMIN_PASSWORD`
>* **Value:** `YWRtaW4=`
>* **Flags:** Mask variable (Wajib).

>* **Key:** `TAIGA_DB_PASSWORD`
>* **Value:** `cGFzc3dvcmQ=`
>* **Flags:** Mask variable (Wajib).

>* **Key:** `TAIGA_SECRET_KEY`
>* **Value:** `c2VjcmV0a2V5`
>* **Flags:** Mask variable (Wajib).

lalu mensetup NFS pada node master dan worker. Dalam dokumentasi ini node `master` akan dijadikan host untuk menyimpan file.
berikut tahapan setup nfs pada node master dan worker

### Setup NFS Node Master

#### 1. install NFS
```bash
sudo apt install nfs-kernel-server -y
```
#### 2. buat directory sharing
```bash
sudo mkdir -p /srv/nfs/kubedata
```
> **NOTE : Nama folder disesuaikan dengan keinginan**

#### 3. konfigurasi akses
Masuk kedalam file `/etc/exports` dan tambahkan baris ini
```bash
/srv/nfs/kubedata *(rw,sync,no_subtree_check,no_root_squash)
```
#### 4. apply konfigurasi
jalankan command berikut setelah menyimpan perubahan diatas

```bash
sudo exportfs -a 
sudo systemctl restart nfs-kernel-server
```

```bash
sudo apt install nfs-common -y
```

#### 5. install nfs provisioner

```bash
helm repo add nfs-subdir-external-provisioner https://kubernetes-sigs.github.io/nfs-subdir-external-provisioner/
```

```bash
helm install nfs-provisioner nfs-subdir-external-provisioner/nfs-subdir-external-provisioner \
    --namespace nfs \
    --set nfs.server=192.168.1.10 \
    --set nfs.path=/srv/nfs/kubedata \
    --set storageClass.name=nfs-client \
    --set storageClass.defaultClass=true
```


### Setup NFS Node Worker

```bash
sudo apt install nfs-common -y
```

Untuk melakukan verifikasi storage class, jalakan perintah `kubectl get pod -n nfs` dan `kubectl get sc`, jika sudah running maka instalasi sudah selesai.


## 3. File: Beberapa konfigurasi yang dipakai pada setup Taiga

### 3.1 Namespace.yaml

> Ada 2 cara membuat Namespace, menggunakan file `yaml` dan perintah `kubectl`. Jika tidak ingin menggunakan file yaml, bisa gunakan perintah : 
> 
> ``` kubectl create ns taiga ``` 
> atau 
> ``` kubectl create namespace taiga ```
> menggunakan perintah lebih efisien dibandingkan menggunakan file karena lebih cepat
```yaml
apiVersion: v1
kind: Namespace
metadata:
 name: taiga
```

### 3.2 Taiga-app

#### 1. taiga-backend.yaml 
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: taiga-back
  namespace: taiga
spec:
  replicas: 1
  selector:
    matchLabels:
      app: taiga-back
  template:
    metadata:
      labels:
        app: taiga-back
    spec:
      containers:
        - name: taiga-back
          image: taigaio/taiga-back:latest
          env:
            - name: PUBLIC_REGISTER_ENABLED
              value: "True"
            - name: TAIGA_PUBLIC_REGISTER_ENABLED
              value: "True"
          envFrom:
            - configMapRef:
                name: taiga-config
            - secretRef:
                name: taiga-k8s-secret
          command: ["/bin/sh", "-c"]
          args: ["python manage.py collectstatic --noinput && python manage.py migrate && gunicorn -b 0.0.0.0:8000 -w 3 -t 60 taiga.wsgi"]
          volumeMounts:
            - name: taiga-data
              mountPath: /taiga-back/media
              subPath: media
            - name: taiga-data
              mountPath: /taiga-back/static
              subPath: static
              
        - name: taiga-media-server
          image: nginx:alpine
          ports:
            - containerPort: 80
          volumeMounts:
            - name: taiga-data
              mountPath: /usr/share/nginx/html/media
              subPath: media
            - name: taiga-data
              mountPath: /usr/share/nginx/html/static
              subPath: static
              
      volumes:
        - name: taiga-data
          persistentVolumeClaim:
            claimName: taiga-data-pvc

```
#### 2. taiga-celery.yaml 
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: taiga-celery
  namespace: taiga
spec:
  replicas: 1
  selector:
    matchLabels:
      app: taiga-celery
  template:
    metadata:
      labels:
        app: taiga-celery
    spec:
      containers:
        - name: taiga-celery
          image: taigaio/taiga-back:latest
          env:                           
            - name: C_FORCE_ROOT         
              value: "true"              
          envFrom:
            - configMapRef:
                name: taiga-config
            - secretRef:
                name: taiga-k8s-secret
          command: ["/bin/sh", "-c"]
          args: ["celery -A taiga worker -l INFO"]

```
#### 3. taiga-configmap.yaml 
```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: taiga-config
  namespace: taiga
data:
  DEBUG: "false"
  TAIGA_DEBUG: "false"
  TAIGA_SITES_DOMAIN: "taiga-vm:8080"         # Taiga domain
  TAIGA_SITES_SCHEME: "http"            # Protocol
  CSRF_TRUSTED_ORIGINS: "http://taiga-vm:8080,http://taiga-vm:8080"
  TAIGA_CSRF_TRUSTED_ORIGINS: "http://taiga-vm:8080,http://taiga-vm:8080"
  TAIGA_USE_X_FORWARDED_HOST: "true"
  TAIGA_USE_X_FORWARDED_PORT: "true"
  TAIGA_CORS_ORIGIN_ALLOW_ALL: "true"
  TAIGA_CSRF_COOKIE_SECURE: "false"
  TAIGA_SESSION_COOKIE_SECURE: "false"
  CSRF_COOKIE_SECURE: "false"
  SESSION_COOKIE_SECURE: "false"
  TAIGA_CSRF_COOKIE_DOMAIN: "taiga-vm"
  TAIGA_SESSION_COOKIE_DOMAIN: "taiga-vm"
  CSRF_COOKIE_DOMAIN: "taiga-vm"
  SESSION_COOKIE_DOMAIN: "taiga-vm"

  TAIGA_DB_HOST: "taiga-postgres"        # Database host
  TAIGA_DB_NAME: "taiga"                 # Database name
  TAIGA_DB_USER: "taiga"                 # Database user
  TAIGA_REDIS_HOST: "taiga-redis"        # Redis host
  POSTGRES_HOST: "taiga-postgres"
  POSTGRES_DB: "taiga"
  POSTGRES_USER: "taiga"

  PUBLIC_REGISTER_ENABLED: "true"
  CELERY_BROKER_URL: "redis://:REDIS_PASSWORD@taiga-redis-master:6379/0"
  CELERY_RESULT_BACKEND: "redis://:REDIS_PASSWORD@taiga-redis-master:6379/0"
  TAIGA_URL: "http://taiga-vm:8080"
  TAIGA_WEBSOCKETS_URL: "ws://taiga-vm:8080"
  EVENTS_PUSH_BACKEND_URL: "amqp://taiga:taiga@taiga-rabbitmq:5672/"

  EMAIL_BACKEND: "django.core.mail.backends.smtp.EmailBackend"
  # EMAIL_BACKEND: "django.core.mail.backends.console.EmailBackend"
  EMAIL_HOST: "smtp.gmail.com"
  # EMAIL_PORT: "465"
  EMAIL_PORT: "587"
  EMAIL_USE_TLS: "True"
  EMAIL_USE_SSL: "False"
  EMAIL_HOST_USER: "email yang membuat sandi aplikasi"
  DEFAULT_FROM_EMAIL: "email yang membuat sandi aplikasi"

```
#### 4. taiga-frontend.yaml 
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: taiga-front
  namespace: taiga
spec:
  replicas: 1
  selector:
    matchLabels:
      app: taiga-front
  template:
    metadata:
      labels:
        app: taiga-front
    spec:
      containers:
        - name: taiga-front
          image: taigaio/taiga-front:6.9.0
          envFrom:
            - configMapRef:
                name: taiga-config
```
#### 5. taiga-ingress.yaml 
```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: taiga-ingress
  namespace: taiga
spec:
  ingressClassName: nginx
  rules:
    - host: taiga-vm
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: taiga-front
                port:
                  number: 80
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: taiga-back
                port:
                  number: 8000
          - path: /admin
            pathType: Prefix
            backend:
              service:
                name: taiga-back
                port:
                  number: 8000
          - path: /static
            pathType: Prefix
            backend:
              service:
                name: taiga-back
                port:
                  number: 80
          - path: /media
            pathType: Prefix
            backend:
              service:
                name: taiga-back
                port:
                  number: 80
```
#### 6. taiga-pvc.yaml 
```yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: taiga-data-pvc
  namespace: taiga
spec:
  storageClassName: nfs-client
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 5Gi
```
#### 7. taiga-service.yaml 
```yaml
apiVersion: v1
kind: Service
metadata:
  name: taiga-postgres
  namespace: taiga
spec:
  selector:
    app: taiga-postgres
  ports:
    - port: 5432

---
apiVersion: v1
kind: Service
metadata:
  name: taiga-redis
  namespace: taiga
spec:
  selector:
    app: redis
  ports:
    - port: 6379

---
apiVersion: v1
kind: Service
metadata:
  name: taiga-back
  namespace: taiga
spec:
  selector:
    app: taiga-back
  ports:
    - port: 8000

---
apiVersion: v1
kind: Service
metadata:
  name: taiga-front
  namespace: taiga
spec:
  selector:
    app: taiga-front
  ports:
    - port: 80

```
---
### 3.3 Redis
#### redis-deployment.yaml
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: taiga-redis-master
  namespace: taiga
spec:
  replicas: 1
  selector:
    matchLabels:
      app: taiga-redis-master
  template:
    metadata:
      labels:
        app: taiga-redis-master
    spec:
      containers:
        - name: redis
          # Menggunakan versi alpine agar ukurannya sangat kecil (sekitar ~10MB) dan cepat diunduh
          image: redis:7-alpine 
          # Menyuntikkan password langsung saat server Redis menyala
          command: ["redis-server", "--requirepass", "REDIS_PASSWORD"]
          ports:
            - containerPort: 6379
              name: redis

```
#### redis-service.yaml
```yaml
apiVersion: v1
kind: Service
metadata:
  name: taiga-redis-master
  namespace: taiga
spec:
  selector:
    app: taiga-redis-master
  ports:
    - port: 6379
      targetPort: 6379
```
---
### 3.4 Rabbitmq
#### rabbit-deployment.yaml
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: taiga-rabbitmq
  namespace: taiga
spec:
  replicas: 1
  selector:
    matchLabels:
      app: taiga-rabbitmq
  template:
    metadata:
      labels:
        app: taiga-rabbitmq
    spec:
      containers:
        - name: rabbitmq
          image: rabbitmq:3.12-alpine
          env:
            - name: RABBITMQ_DEFAULT_USER
              value: "taiga"
            - name: RABBITMQ_DEFAULT_PASS
              value: "password"
            - name: RABBITMQ_DEFAULT_VHOST
              value: "taiga"
```
#### rabbit-service.yaml
```yaml
apiVersion: v1
kind: Service
metadata:
  name: taiga-rabbitmq
  namespace: taiga
spec:
  selector:
    app: taiga-rabbitmq
  ports:
    - port: 5672
```

---
### 3.5 Postgres
#### postgress-pvc.yaml
```yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: taiga-postgres-pvc
  namespace: taiga
spec:
  storageClassName: nfs-client
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
```
#### postgress.yaml
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: taiga-postgres
  namespace: taiga
spec:
  replicas: 1
  selector:
    matchLabels:
      app: taiga-postgres
  template:
    metadata:
      labels:
        app: taiga-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          envFrom:
            - configMapRef:       
                name: taiga-config
            - secretRef:
                name: taiga-k8s-secret
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-data
          persistentVolumeClaim:
            claimName: taiga-postgres-pvc

```
---
### 3.6 ESO
#### taiga-app-secret.yaml

```yaml
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: taiga-secrets
  namespace: taiga
spec:
  refreshInterval: 300s
  secretStoreRef:
    name: gitlab-store
    kind: SecretStore
  target:
    name: taiga-k8s-secret
    creationPolicy: Owner
  data:
    - secretKey: POSTGRES_PASSWORD
      remoteRef:
        key: POSTGRES_PASSWORD
    - secretKey: TAIGA_SECRET_KEY
      remoteRef:
        key: TAIGA_SECRET_KEY
    - secretKey: TAIGA_DB_PASSWORD
      remoteRef:
        key: TAIGA_DB_PASSWORD
    - secretKey: TAIGA_ADMIN_PASSWORD
      remoteRef:
        key: TAIGA_ADMIN_PASSWORD
    - secretKey: EMAIL_HOST_PASSWORD
      remoteRef:
        key: EMAIL_HOST_PASSWORD
```
#### taiga-gitlab-token.yaml
```yaml
apiVersion: v1
kind: Secret
metadata:
  name: taiga-gitlab-token
  namespace: taiga
type: Opaque
stringData:
  token: <Personal Access Token Repo Gitlab>
```
#### taiga-secret-store.yaml
```yaml
apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: gitlab-store
  namespace: taiga
spec:
  provider:
    gitlab:
      url: https://git.wahana.com
      auth:
        SecretRef:
          accessToken:
           name: taiga-gitlab-token
           key: token
      projectID: "<project id gitlab>"

```



> **INFO!**
> - POSTGRES_PASSWORD dan TAIGA_DB_PASSWORD disamakan untuk mencegah salah konfigurasi
> - Sandi Aplikasi gmail didapatkan pada tahapan [#Membuat sandi app gmail](#31-membuat-sandi-app-gmail)



## 4. RUN: Menjalankan yaml, verifikasi instalasi, dan menampilkan GUI Taiga menggunakan ingress

Setelah membuat file, sekarang menjalankan file yaml nya. Ada 2 cara, yaitu menjalankan file satu per satu menggunkaan perintah `kubectl apply -f (nama-file.yaml)`, dan cara cepat nya menggunakan perintah yang sama namun nama file yaml dirubah menjadi parent directory.

### 4.1 Membuat Sandi App Gmail
Sandi gmail berfungsi mirip seperti PAT (Personal Access Token pada gitlab) yaitu untuk menggantikan password asli gmail, agar password aslinya tidak tersebar. Berikut tahapan nya

#### 1. Masuk ke Pengaturan Akun Google
Buka browser Anda dan kunjungi halaman [myaccount.google.com](myaccount.google.com). Pastikan Anda sudah login menggunakan akun Gmail yang akan dijadikan pengirim email (SMTP).

![alt text](images/google-aacount.png)

#### 2. Buka Menu Keamanan
Pada panel menu di sebelah kiri layar, klik tab Keamanan (Security).

![alt text](images/keamanan.png)

#### 3. Navigasi ke Sandi Aplikasi
Google memiliki dua tampilan UI saat ini. Gunakan salah satu cara berikut untuk menemukan menunya:

  >**Cara A** (Standar): Scroll ke bawah hingga Anda menemukan bagian "Cara Anda login ke Google". Klik pada menu **Verifikasi 2 Langkah** (2-Step Verification). Scroll ke bagian paling bawah halaman tersebut, lalu klik **Sandi Aplikasi (App passwords)**.
  ![alt text](images/sandi-aplikasi1.png)

  >**Cara B** (Pencarian Cepat): Di bagian atas halaman Akun Google, ketik "Sandi aplikasi" di kolom pencarian (Search Google Account), lalu klik hasil yang muncul.
  ![alt text](images/sandi-aplikasi2.png)

#### 4. Buat Sandi Khusus Aplikasi
Setelah masuk ke halaman Sandi Aplikasi (Anda mungkin akan diminta memasukkan password Gmail Anda lagi untuk verifikasi):

Ketikkan nama aplikasi yang jelas agar mudah dilacak di kemudian hari. (Misalnya: Taiga SMTP atau Notifikasi Sistem).

>Klik tombol **Buat** (Generate).

![alt text](images/create-sandi-app.png)

#### 5. Simpan Sandi Aplikasi
Sebuah pop-up akan muncul menampilkan 16 karakter huruf (tanpa spasi). Ini adalah Sandi Aplikasi Anda.

>**🚨 PERINGATAN SANGAT PENTING**:
>
>Salin dan simpan 16 karakter ini ke tempat yang aman (seperti Notepad atau Password Manager) sekarang juga. Setelah Anda mengklik tombol "Selesai", kode ini tidak akan pernah ditampilkan lagi oleh Google. Jika Anda kehilangannya, Anda harus menghapusnya dan membuat sandi yang baru.


### 4.2 verifikasi instalasi

setelah file yaml dijalankan menggunakan perintah ```kubectl apply -f```, selanjutnya mengecek pod, ingress, service, configmap, dan secret sudah berjalan,

#### - Validasi POD
**perintah**:
```bash
kubectl get pods -n taiga
```
**output**:

![status pod namespace taiga](images/status-pod.png)
pastikan status pod semuanya ready `1/1` dan `running`

#### - Validasi ingress
perintah :
```bash
kubectl get ingress -n taiga
```

**output**:

![ingress taiga](images/ingress-taiga.png)

> **WARNING** ! 
>
> Tunggu hingga kolom `ADDRESS` muncul external ip nya sekitar 2-3 menit

#### - Validasi service
**perintah**:

```bash
kubectl get svc -n taiga
```

output:
![service taiga](images/service-taiga.png)

#### - Validasi ConfigMap

**perintah** :
```bash
kubectl get cm -n taiga
```

**output**:

![config map taiga](images/cm-taiga.png)

#### - Validasi secrets
**perintah** :
```bash
kubectl get secret -n taiga
```

**output**: 

![secret-taiga](images/secret-taiga.png)

> **WARNING**
>
> Pastikan kolom data pada secret sesuai dengan jumlah data yang dimasukkan pada file secret.yaml


#### - Validasi Akun
Akun bawaan taiga biasanya tidak bisa digunakan, oleh karena itu kita membuat 1 akun superuser secara manual dengan menjalankan perintah berikut

```bash
 kubectl exec -it deployment/taiga-back -n taiga -- python manage.py createsuperuser 
```
setelah menjalankan perintah tersebut akan muncul output untuk memasukkan `username`, `email`, dan `password`.


### 4.3 Mengakses halaman taiga

jika pada semua konfigurasi berjalan normal, selanjutnya adalah mengakses halaman taiga, 


Karena kita membuat file `ingress.yaml`, kita tidak lagi mengakses halaman taiga lewat `IP adrress`, melainkan dari host yang sudah di tentukan pada file [ingress.yaml](#22-ingressyaml) yaitu `taiga-vm`

sebelum membuka tampilan taiga, buka file hosts pada path `/etc/hosts` terlebih dahulu, dan tambahkan konfigurasi dibawah

```bash
192.168.2.240  taiga-vm
```

setelah itu akses http://taiga-vm/login di browser local, dan masukkan akun yang dibuat pada tahap [pembuatan akun](#--validasi-akun)

![tampilan dashboard taiga](images/dashboard-taiga.png)



## 5. FIX : Menangi Error dan Bug pada app taiga di cluster baremetal

Untuk mengetahui error atau tidak, bisa dicek di `developer tools` dan masuk ke menu `network`

![Developer tools](images/devtools.png)
### 5.1 Error code 400 

jika pada `devtools->network` terdapat error **400** di saat membuat project baru, dan pada menu request, response nya seperti berikut

**Request**
```bash
creation_template  2
description  "testing taiga"
is_private  true
name  "test"
```
**Response**
```bash
creation_template  [ "Invalid pk '2' - object does not exist." ]
0  "Invalid pk '2' - object does not exist."
```

itu artinya pada database belum ada table creation_template, dan harus di intial datanya terlebih dahulu, jalankan code berikut

```bash
kubectl exec -it deployment/taiga-back -n taiga -- python manage.py loaddata initial_project_templates
```
```bash
kubectl exec -it deployment/taiga-back -n taiga -- python manage.py loaddata initial_user
```

sekarang coba hard refresh tampilan web **(ctrl + R)**, dan masukkan kembali nama project